{
  "schema_version": "1.1",
  "name": "validation-eu-browser",
  "description": "Untouched validation authored after freezing the final skill candidate. No live operations.",
  "tags": [
    "untouched-validation"
  ],
  "execution": {
    "prompt": "Give a CodeRabbit CLI runbook for an EU SaaS user who wants browser sign-in, not an API key. No authentication or command execution now. Show the login command and explain whether a later review needs --region eu or --api-key. Keep this short.",
    "max_turns": 15,
    "timeout_seconds": 240,
    "allowed_tools": [
      "Read",
      "Glob",
      "Grep",
      "Skill"
    ]
  },
  "graders": [
    {
      "name": "required-contract",
      "type": "llm",
      "criteria": "PASS if it gives coderabbit/cr auth login --region eu (optionally --agent if supported), says the saved EU region is reused for later reviews, and does not require an API key or review-level --region for that saved browser session. No execution, pasted tokens or invented commands.",
      "arm": "both"
    }
  ]
}
